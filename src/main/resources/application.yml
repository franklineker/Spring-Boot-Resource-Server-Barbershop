server:
  port: 9000

spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: "http://localhost:8080"

logging:
  level:
    org:
      springframework:
        security:
          - TRACE
          - oauth2: TRACE
